hello bạn có những skill nào

Chào bạn! Mình có các skill sau đây:

Phân tích & Giám sát hệ thống:

database-oracle-awr — Phân tích báo cáo AWR của Oracle (top SQL, execution plan, wait events, SGA/PGA advisory, bottleneck). Chỉ dùng khi bạn nhắc đến AWR report/file.
observability-graylog — (được nhắc đến trong mô tả) dùng cho giám sát real-time, log gần đây, kiểm tra lỗi DB.
Nghiên cứu & Xác minh:

deep-research — Nghiên cứu chuyên sâu, đa nguồn, có trích dẫn trên web.
verify — Xác minh code change hoạt động đúng như mong đợi.
code-review — Review diff hiện tại tìm bug và cải tiến.
security-review — Review bảo mật các thay đổi đang chờ.
Cấu hình & Tối ưu môi trường:

update-config — Cấu hình Claude Code harness qua settings.json (permissions, env vars, hooks).
keybindings-help — Tùy chỉnh phím tắt trong ~/.claude/keybindings.json.
fewer-permission-prompts — Giảm số lần hỏi permission bằng allowlist.
Tự động hóa & Chạy ứng dụng:

loop — Chạy lặp lại một prompt/lệnh theo chu kỳ (vd: poll status mỗi 5 phút).
run — Chạy/khởi động app của dự án để xem thay đổi hoạt động.
init — Khởi tạo file CLAUDE.md cho codebase.
simplify — Review và áp dụng các cải tiến về reuse/simplification.
Ngoài ra mình còn có các công cụ cơ bản như Bash, Read/Write/Edit file, Grep/Glob tìm kiếm, WebSearch/WebFetch, và Agent để giao việc cho sub-agent.

Bạn cần mình hỗ trợ gì cụ thể không? 😊